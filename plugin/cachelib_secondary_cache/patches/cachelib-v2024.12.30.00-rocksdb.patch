diff --git a/cachelib/CMakeLists.txt b/cachelib/CMakeLists.txt
index 581ac8a2..6f0381a8 100644
--- a/cachelib/CMakeLists.txt
+++ b/cachelib/CMakeLists.txt
@@ -43,6 +43,8 @@ set(PACKAGE_BUGREPORT "https://github.com/facebook/TBD")
 set(CMAKE_POSITION_INDEPENDENT_CODE ON)
 
 option(BUILD_TESTS "If enabled, compile the tests." ON)
+option(CACHELIB_BUILD_NAVY_ONLY
+       "Build only the Common and Navy libraries." OFF)
 
 
 set(BIN_INSTALL_DIR bin CACHE STRING
@@ -97,7 +99,9 @@ find_package(Boost REQUIRED COMPONENTS
              system filesystem regex context program_options thread)
 find_package(Gflags REQUIRED)
 find_package(Glog REQUIRED)
-find_package(GTest CONFIG REQUIRED)
+if (BUILD_TESTS)
+  find_package(GTest CONFIG REQUIRED)
+endif()
 find_package(folly CONFIG REQUIRED)
 find_package(fizz CONFIG REQUIRED)
 find_package(fmt CONFIG REQUIRED)
@@ -105,8 +109,10 @@ find_package(wangle CONFIG REQUIRED)
 find_package(Zlib REQUIRED)
 find_package(Zstd REQUIRED)
 find_package(FBThrift REQUIRED) # must come after wangle
-find_package(NUMA REQUIRED)
-find_package(Sparsemap REQUIRED)
+if (NOT CACHELIB_BUILD_NAVY_ONLY)
+  find_package(NUMA REQUIRED)
+  find_package(Sparsemap REQUIRED)
+endif()
 
 find_package(uring)
 if (NOT uring_FOUND)
@@ -284,23 +290,21 @@ endfunction()
 
 
 add_subdirectory (common)
-add_subdirectory (shm)
 add_subdirectory (navy)
-add_subdirectory (allocator)
-add_subdirectory (datatype)
-add_subdirectory (compact_cache)
-add_subdirectory (benchmarks)
-add_subdirectory (cachebench)
+if (NOT CACHELIB_BUILD_NAVY_ONLY)
+  add_subdirectory (shm)
+  add_subdirectory (allocator)
+  add_subdirectory (datatype)
+  add_subdirectory (compact_cache)
+  add_subdirectory (benchmarks)
+  add_subdirectory (cachebench)
+endif()
 
 # Install the source header files
 install(
   DIRECTORY
-    allocator
     common
-    compact_cache
-    datatype
     navy
-    shm
   DESTINATION ${INCLUDE_INSTALL_DIR}
   FILES_MATCHING PATTERN "*.h"
   PATTERN "external" EXCLUDE
@@ -347,25 +351,26 @@ install(
   DESTINATION ${CONFIGS_INSTALL_DIR}
 )
 
-add_library(cachelib INTERFACE)
-target_link_libraries(cachelib INTERFACE
-     cachelib_common
-     cachelib_shm
-     cachelib_navy
-     cachelib_allocator
-     )
-target_include_directories(
-  cachelib
-  INTERFACE
-    $<INSTALL_INTERFACE:${INCLUDE_INSTALL_DIR}>
-    )
 target_compile_features(cachelib_common INTERFACE cxx_std_17)
-
-install(TARGETS
-     cachelib
-     EXPORT cachelib-exports
-     DESTINATION ${LIB_INSTALL_DIR}
-  )
+if (NOT CACHELIB_BUILD_NAVY_ONLY)
+  add_library(cachelib INTERFACE)
+  target_link_libraries(cachelib INTERFACE
+       cachelib_common
+       cachelib_shm
+       cachelib_navy
+       cachelib_allocator
+       )
+  target_include_directories(
+    cachelib
+    INTERFACE
+      $<INSTALL_INTERFACE:${INCLUDE_INSTALL_DIR}>
+      )
+  install(TARGETS
+       cachelib
+       EXPORT cachelib-exports
+       DESTINATION ${LIB_INSTALL_DIR}
+    )
+endif()
 
 install(EXPORT cachelib-exports
         FILE cachelib-targets.cmake
@@ -373,17 +378,18 @@ install(EXPORT cachelib-exports
         DESTINATION ${CMAKE_INSTALL_DIR})
 
 if (BUILD_SHARED_LIBS)
-  set_target_properties(
-    cachelib_allocator
-    cachelib_cachebench
-    cachelib_common
-    cachelib_datatype
-    cachelib_navy
-    cachelib_shm
+  set(CACHELIB_VERSIONED_TARGETS cachelib_common cachelib_navy)
+  if (NOT CACHELIB_BUILD_NAVY_ONLY)
+    list(APPEND CACHELIB_VERSIONED_TARGETS
+         cachelib_allocator
+         cachelib_cachebench
+         cachelib_datatype
+         cachelib_shm)
+  endif()
+  set_target_properties(${CACHELIB_VERSIONED_TARGETS}
     PROPERTIES
-    SOVERSION ${CACHELIB_MAJOR_VERSION}
-    VERSION ${PACKAGE_VERSION}
-  )
+      SOVERSION ${CACHELIB_MAJOR_VERSION}
+      VERSION ${PACKAGE_VERSION})
 endif ()
 
 if (BUILD_TESTS)
diff --git a/cachelib/common/CMakeLists.txt b/cachelib/common/CMakeLists.txt
index 927f2fa3..f07a646c 100644
--- a/cachelib/common/CMakeLists.txt
+++ b/cachelib/common/CMakeLists.txt
@@ -36,10 +36,6 @@ target_link_libraries(cachelib_common PUBLIC
   FBThrift::thriftcpp2
   FBThrift::thriftprotocol
   Folly::folly
-  Folly::follybenchmark
-  Folly::folly_exception_tracer
-  Folly::folly_exception_tracer_base
-  Folly::folly_exception_counter
 )
 
 install(TARGETS cachelib_common
diff --git a/cachelib/common/Utils.cpp b/cachelib/common/Utils.cpp
index 57a1beb9..204a432b 100644
--- a/cachelib/common/Utils.cpp
+++ b/cachelib/common/Utils.cpp
@@ -15,7 +15,6 @@
  */
 
 #include <dirent.h>
-#include <folly/debugging/exception_tracer/ExceptionTracer.h>
 #include <sys/mman.h>
 #include <sys/resource.h>
 #include <sys/stat.h>
@@ -428,10 +427,6 @@ size_t getMemAvailable() {
 }
 
 void printExceptionStackTraces() {
-  auto exceptions = folly::exception_tracer::getCurrentExceptions();
-  for (auto& exc : exceptions) {
-    std::cerr << exc << std::endl;
-  }
 }
 
 } // namespace util
diff --git a/cachelib/navy/CMakeLists.txt b/cachelib/navy/CMakeLists.txt
index 6c99ea88..6c02f0df 100644
--- a/cachelib/navy/CMakeLists.txt
+++ b/cachelib/navy/CMakeLists.txt
@@ -44,7 +44,6 @@ add_library (cachelib_navy
   scheduler/ThreadPoolJobScheduler.cpp
   scheduler/ThreadPoolJobQueue.cpp
   serialization/RecordIO.cpp
-  testing/MockDevice.cpp
   )
 add_dependencies(cachelib_navy thrift_generated_files)
 
@@ -58,7 +57,6 @@ endif()
 
 target_link_libraries(cachelib_navy PUBLIC
   cachelib_common
-  GTest::gmock
   )
 
 install(TARGETS cachelib_navy
diff --git a/cachelib/navy/serialization/RecordIO.cpp b/cachelib/navy/serialization/RecordIO.cpp
index f5d83250..ae82852f 100644
--- a/cachelib/navy/serialization/RecordIO.cpp
+++ b/cachelib/navy/serialization/RecordIO.cpp
@@ -74,7 +74,7 @@ class DeviceMetaDataWriter final : public RecordWriter {
     uint8_t* bufferData = buffer_.data();
     // Write the last remaining bytes to the device
     if (bufIndex_ > 0) {
-      if (offset_ + blockSize_ < metadataSize_) {
+      if (offset_ + blockSize_ <= metadataSize_) {
         Buffer buffer = dev_.makeIOBuffer(blockSize_);
         memcpy(buffer.data(), bufferData, bufIndex_);
         memset(buffer.data() + bufIndex_, 0, blockSize_ - bufIndex_);
diff --git a/contrib/build-package.sh b/contrib/build-package.sh
index 406031bd..81d153eb 100755
--- a/contrib/build-package.sh
+++ b/contrib/build-package.sh
@@ -199,7 +199,7 @@ case "$1" in
     NAME=folly
     SRCDIR=cachelib/external/$NAME
     update_submodules=yes
-    cmake_custom_params="-DBUILD_SHARED_LIBS=ON"
+    cmake_custom_params="-DBUILD_SHARED_LIBS=ON -DFOLLY_NO_EXCEPTION_TRACER=ON"
     if test "$build_tests" = "yes" ; then
         cmake_custom_params="$cmake_custom_params -DBUILD_TESTS=ON"
     else
@@ -235,19 +235,20 @@ case "$1" in
     NAME=mvfst
     SRCDIR=cachelib/external/$NAME
     update_submodules=yes
-    cmake_custom_params="-DBUILD_SHARED_LIBS=ON"
+    cmake_custom_params="-DBUILD_SHARED_LIBS=ON -DBUILD_TESTS=OFF"
     ;;
 
   fbthrift)
     NAME=fbthrift
     SRCDIR=cachelib/external/$NAME
     update_submodules=yes
-    cmake_custom_params="-DBUILD_SHARED_LIBS=ON"
+    cmake_custom_params="-DBUILD_SHARED_LIBS=ON -DBUILD_TESTS=OFF -Denable_tests=OFF"
     ;;
 
   cachelib)
     NAME=cachelib
     SRCDIR=cachelib
+    cmake_custom_params="-DCACHELIB_BUILD_NAVY_ONLY=ON"
     if test "$build_tests" = "yes" ; then
         cmake_custom_params="$cmake_custom_params -DBUILD_TESTS=ON"
     else
@@ -271,7 +272,7 @@ test "$debug_build" \
 MAKE_PARAMS=
 test "$verbose" && MAKE_PARAMS="$MAKE_PARAMS VERBOSE=YES"
 
-JOBS=$(nproc --ignore 1)
+JOBS=${CACHELIB_JOBS:-$(nproc --ignore 1)}
 test "$many_jobs" && MAKE_PARAMS="$MAKE_PARAMS -j$JOBS"
 
 
